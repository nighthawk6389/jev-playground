# Leave everything unset to run the fully playable fixture demo.

# Live Jev judgments. Get a key at https://typesafe.ai
TYPESAFE_API_KEY=

# "fixture" (default when no API key) or "live"
DATA_MODE=fixture

# Optional overrides — used by the offline test harness to exercise the LIVE
# code path against a local fixture server.
# TYPESAFE_BASE_URL=https://api.typesafe.ai
# GAMMA_BASE_URL=https://gamma-api.polymarket.com
